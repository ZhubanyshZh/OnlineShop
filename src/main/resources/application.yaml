server:
  port: 7777

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: MensShop
    url: jdbc:postgresql://localhost:5432/postgres
  jpa:
    hibernate:
      ddl-auto: update